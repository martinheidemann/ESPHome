one_wire:
  - platform: gpio
    pin: $temperature_pin

sensor:
  - platform: dallas_temp
    address: $temperature_addr_1
    name: Temperatur

    filters:
      - sliding_window_moving_average:
          window_size: 3
          send_every: 3
          send_first_at: 3
